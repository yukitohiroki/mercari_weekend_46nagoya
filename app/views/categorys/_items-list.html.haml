.bread-crumbs
.breads
  - breadcrumb :item_search_firstcategory
  = breadcrumbs separator: "#{content_tag(:i, '', class: 'fa fa-caret-right')}"
.list-container
  .full-width
    %ul.parts-nav.clearfix
    %section.items-box-container.clearfix
      %h1.items-box-head
        #{@first_category.first_category}の商品一覧
      .items-box-content.clearfix
        - @items.where(first_category_id: @first_category.id).each do |item|
          = render partial: 'categorys/item-box', locals: {item:item}
        %end
    .pager
      = paginate @items
    .under-decoration
      .text-red
        #{@first_category.first_category}
      .text-white
        %p
          メルカリなら豊富な商品の中から欲しかった商品が見つかるかも！？