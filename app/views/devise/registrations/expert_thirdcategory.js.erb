if($("#user_second_category_id").val() !== "" ){
  $('#third-select').html(
    "<div><div class=select--wrap><%= j(select :user, :third_category_id, options_from_collection_for_select(@thirdcategory, :id, :third_category),{prompt: '---'}) %></div></div>"
  );
}else{
  $('#third-select').html(
    ""
  );
}